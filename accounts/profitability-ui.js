(()=>{'use strict';const q=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=n=>Number(n||0).toLocaleString('en-PK',{minimumFractionDigits:2,maximumFractionDigits:2}),entity=()=>localStorage.getItem('tt_accounts_entity')||'TTI';async function open(button){q('#ttReportsPanel')?.querySelectorAll('[data-rpt]').forEach(x=>x.classList.remove('active'));button.classList.add('active');const box=q('#ttReportsBody');box.textContent='Loading shipment profitability…';try{const r=await fetch('../api/shipment_profitability.php?entity='+encodeURIComponent(entity()),{credentials:'same-origin'}),d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'Could not load shipment profitability.');box.innerHTML=`<div class="notice">${esc(d.warning)}</div><div class="tableWrap"><table><thead><tr><th>Contract / Lot</th><th>Revenue</th><th>Stock Cost</th><th>Transport</th><th>Freight</th><th>Clearing</th><th>Fumigation</th><th>Inspection / Other</th><th>Total Cost</th><th>Contribution</th></tr></thead><tbody>${d.rows.map(x=>`<tr><td><b>${esc(x.contractRef||'—')}</b><br>${esc(x.lotRef||x.key)}</td><td>${fmt(x.revenue)}</td><td>${fmt(x.stockCost)}</td><td>${fmt(x.transport)}</td><td>${fmt(x.freight)}</td><td>${fmt(x.clearing)}</td><td>${fmt(x.fumigation)}</td><td>${fmt(Number(x.inspection||0)+Number(x.otherPostedCost||0))}</td><td>${fmt(x.totalCost)}</td><td><b>${fmt(x.grossContribution)}</b></td></tr>`).join('')||'<tr><td colspan="10">No posted shipment results yet.</td></tr>'}</tbody></table></div>`}catch(e){box.textContent=e.message}}function install(){const tabs=q('#ttReportsPanel .masterTabs');if(!tabs||q('[data-rpt-profit]'))return;const b=document.createElement('button');b.className='tab';b.dataset.rptProfit='1';b.textContent='Shipment Profitability';b.onclick=()=>open(b);tabs.appendChild(b)}new MutationObserver(install).observe(document.documentElement,{childList:true,subtree:true});install()})();
