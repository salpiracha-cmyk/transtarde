(()=>{
  'use strict';
  const access=window.TT_ACCOUNT_ACCESS||{};
  const api='../api/local_sales_control.php';
  const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];
  const currentEntity=()=>localStorage.getItem('tt_accounts_entity')||'TTI';
  let data={summary:null,pending:[],policy:{}},loading=false;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const fmt=n=>Number(n||0).toLocaleString('en-PK',{maximumFractionDigits:2});
  const pct=n=>n==null?'—':Number(n).toFixed(2)+'%';
  function toast(message,ok=true){let e=q('#ttLocalSalesToast');if(!e){e=document.createElement('div');e.id='ttLocalSalesToast';e.style.cssText='position:fixed;left:50%;bottom:22px;transform:translateX(-50%);z-index:100004;padding:11px 15px;border-radius:10px;color:#fff;font:700 12px Arial;box-shadow:0 8px 25px #0003';document.body.appendChild(e)}e.style.background=ok?'#147a5b':'#a93a34';e.textContent=message;e.hidden=false;clearTimeout(e._t);e._t=setTimeout(()=>e.hidden=true,3600)}
  function style(){if(q('#ttLocalSalesStyle'))return;const s=document.createElement('style');s.id='ttLocalSalesStyle';s.textContent=`.ttls-wrap{padding:18px}.ttls-head{display:flex;gap:10px;align-items:flex-start}.ttls-head .sp{flex:1}.ttls-badge{padding:7px 10px;border-radius:999px;background:#eef3f7;color:#40576d;font-size:11px;font-weight:800}.ttls-badge.warn{background:#fff1ce;color:#7b5700}.ttls-badge.stop{background:#fde8e7;color:#9a2823}.ttls-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:12px 0}.ttls-kpi{border:1px solid #dfe6ec;border-radius:10px;padding:11px;background:#fafbfc}.ttls-kpi span{font-size:10px;text-transform:uppercase;color:#697786}.ttls-kpi b{display:block;margin-top:4px;font-size:18px}.ttls-alert{padding:11px 12px;border-radius:9px;margin:10px 0;font-size:12px;background:#eef7f3;color:#285e4c;border:1px solid #d4e8df}.ttls-alert.warn{background:#fff6df;color:#705718;border-color:#ead6a2}.ttls-alert.stop{background:#fdeceb;color:#8c2e29;border-color:#efcbc8}.ttls-columns{display:grid;grid-template-columns:1fr 1.4fr;gap:12px}.ttls-card{border:1px solid #dfe6ec;border-radius:11px;background:#fff;overflow:hidden}.ttls-card h4{margin:0;padding:11px 12px;border-bottom:1px solid #edf0f3;font-size:13px}.ttls-table{width:100%;border-collapse:collapse}.ttls-table th,.ttls-table td{padding:9px 11px;border-bottom:1px solid #edf0f3;text-align:left;font-size:12px}.ttls-table th{font-size:10px;text-transform:uppercase;color:#687686}.ttls-status{display:inline-block;padding:4px 7px;border-radius:999px;background:#fff1ce;color:#7b5700;font-size:10px;font-weight:800}.ttls-actions{display:flex;gap:6px}.ttls-empty{padding:20px;color:#687686;text-align:center;font-size:12px}@media(max-width:900px){.ttls-kpis{grid-template-columns:1fr 1fr}.ttls-columns{grid-template-columns:1fr}}@media(max-width:560px){.ttls-kpis{grid-template-columns:1fr}}`;document.head.appendChild(s)}
  async function load(){if(loading)return;loading=true;try{const r=await fetch(api,{credentials:'same-origin',headers:{Accept:'application/json'}});let d={};try{d=await r.json()}catch{}if(!r.ok||!d.ok)throw new Error(d.error||'Could not load Local Sales control.');data=d}finally{loading=false}}
  function stateClass(){const s=data.summary?.status||'OK';return s==='CRITICAL'?'stop':s==='WARNING'?'warn':''}
  function message(){const s=data.summary;if(!s)return '';if(s.exportSales<=0&&s.localSales>0)return '<b>Critical Local Sales alarm.</b> Local Sales exist but no recognized Pakistan export turnover is available yet for the current financial year. The books remain postable; the exception stays visible for Accounts and owners.';if(s.status==='CRITICAL')return `<b>Critical Local Sales alarm.</b> Current ratio is ${pct(s.ratioPercent)} against the ${pct(s.criticalPercent)} control level. Accounting is not blocked; any new approval requires an exception note and remains in the audit trail.`;if(s.status==='WARNING')return `<b>Local Sales warning.</b> Current ratio is ${pct(s.ratioPercent)}. Warning starts at ${pct(s.warningPercent)} and critical alarm starts at ${pct(s.criticalPercent)}.`;return `Local Sales remain within the current Transtrade monitoring range. Warning starts at <b>${pct(s.warningPercent)}</b>; critical alarm <b>${pct(s.criticalPercent)}</b>.`}
  function products(){const rows=Object.entries(data.summary?.byProduct||{});if(!rows.length)return '<div class="ttls-empty">No recognized Local Sales in this financial year.</div>';return `<table class="ttls-table"><thead><tr><th>By-product / Product</th><th>Recognized Sales</th></tr></thead><tbody>${rows.map(([p,a])=>`<tr><td><b>${esc(p)}</b></td><td>Rs ${fmt(a)}</td></tr>`).join('')}</tbody></table>`}
  function pending(){const rows=data.pending||[];if(!rows.length)return '<div class="ttls-empty">No Local Sales are waiting for Accounts approval.</div>';return `<table class="ttls-table"><thead><tr><th>Soda</th><th>Party</th><th>By-product</th><th>Loaded</th><th>Value</th><th>Status</th><th></th></tr></thead><tbody>${rows.map(c=>`<tr><td><b>${esc(c.soda||c.sourceKey)}</b><div style="font-size:10px;color:#6f7a89">${esc(c.saleDate||'')}</div></td><td>${esc(c.party||'—')}</td><td>${esc(c.product||'—')}</td><td>${fmt(c.loadedKg)} kg</td><td>Rs ${fmt(c.amount)}</td><td><span class="ttls-status">Accounts Pending</span></td><td><div class="ttls-actions"><button class="btn green" data-ttls-approve="${esc(c.id)}">Approve</button><button class="btn" data-ttls-reject="${esc(c.id)}">Return</button></div></td></tr>`).join('')}</tbody></table>`}
  function render(){style();const ws=q('#ws-receivables');if(!ws)return;let root=ws.querySelector('[data-tt-local-sales-root]');if(!root){root=document.createElement('div');root.dataset.ttLocalSalesRoot='1';ws.querySelector('.panelHead')?.insertAdjacentElement('afterend',root)}if(currentEntity()==='TG'){root.innerHTML='<div class="ttls-wrap"><div class="formCard"><h3>Local Sales Control</h3><div class="helper">Pakistan Local Sales monitoring is kept outside TG books. TG customer export turnover is not used to create Local Sales headroom.</div></div></div>';return}const s=data.summary||{},headroom=Number(s.remainingHeadroom||0);root.innerHTML=`<div class="ttls-wrap"><div class="ttls-head"><div><h3 style="margin:0">Local Sales Control</h3><div class="helper">Separate from Export Sales · mandatory by-product tracking · Accounts approval only.</div></div><div class="sp"></div><span class="ttls-badge ${stateClass()}">${pct(s.ratioPercent)} of Export Sales</span></div><div class="ttls-kpis"><div class="ttls-kpi"><span>FY Local Sales</span><b>Rs ${fmt(s.localSales)}</b></div><div class="ttls-kpi"><span>FY Export Sales</span><b>Rs ${fmt(s.exportSales)}</b></div><div class="ttls-kpi"><span>Local / Export</span><b>${pct(s.ratioPercent)}</b></div><div class="ttls-kpi"><span>${headroom>=0?'Headroom to 5%':'Above 5% by'}</span><b>Rs ${fmt(Math.abs(headroom))}</b></div></div><div class="ttls-alert ${stateClass()}">${message()}<div style="margin-top:5px;font-size:10px">Financial year ${esc(s.financialYearFrom||'')} → ${esc(s.financialYearTo||'')} · TG and intercompany sales excluded · internal control pending auditor/legal confirmation.</div></div><div class="ttls-columns"><div class="ttls-card"><h4>By-product-wise Local Sales</h4>${products()}</div><div class="ttls-card"><h4>Pending Accounts Approval</h4>${pending()}</div></div></div>`;bind()}
  function bind(){qa('[data-ttls-approve]').forEach(b=>b.onclick=()=>approve(b.dataset.ttlsApprove));qa('[data-ttls-reject]').forEach(b=>b.onclick=()=>reject(b.dataset.ttlsReject))}
  async function approve(id){const c=(data.pending||[]).find(x=>x.id===id);if(!c)return;const s=data.summary||{},projected=s.exportSales>0?((Number(s.localSales||0)+Number(c.amount||0))/Number(s.exportSales))*100:null,critical=Number(s.criticalPercent||5),isCritical=projected==null||projected>=critical,text=projected==null?'No recognized export turnover available':projected.toFixed(2)+'% projected';let exceptionNote='';if(isCritical){exceptionNote=prompt(`CRITICAL LOCAL SALES ALARM\n\n${c.soda||c.sourceKey} · ${c.product} · Rs ${fmt(c.amount)}\n${text}\n\nThe accounting entry will NOT be blocked. Enter the reason / exception note for the audit trail:`, '')||'';if(!exceptionNote.trim())return toast('A reason is required for a critical Local Sales exception.',false)}else if(!confirm(`Approve Local Sale ${c.soda||c.sourceKey}?\n${c.product} · Rs ${fmt(c.amount)}\n${text}\n\nApproval will post the Local Sale accounting entry.`))return;await act('approve_candidate',id,'',exceptionNote)}
  async function reject(id){const note=prompt('Reason for returning this Local Sale to Mill / operations:','');if(note===null)return;await act('reject_candidate',id,note,'')}
  async function act(action,id,note,exceptionNote){try{const r=await fetch(api,{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({action,csrf:access.csrf,candidateId:id,note,exceptionNote})});let d={};try{d=await r.json()}catch{}if(!r.ok||!d.ok)throw new Error(d.error||'Local Sales update failed.');toast(action==='approve_candidate'?'Local Sale approved and posted.':'Local Sale returned without posting.');await load();render()}catch(e){toast(String(e.message||e),false)}}
  async function mount(){try{await load();render()}catch(e){toast(String(e.message||e),false)}}
  document.addEventListener('click',e=>{if(e.target.closest('.appCard[data-key="receivables"]'))setTimeout(mount,80);if(e.target.closest('[data-tt-entity]'))data={summary:null,pending:[],policy:{}}},false);
  window.TT_LOCAL_SALES_CONTROL_UI={mount};
})();
